# WARNING: This repository is public. Do NOT store secrets here.
# Secrets and keys are managed in GitHub Actions / Supabase project settings.
# Use a local, gitignored `.env.local` for development overrides if needed.

# Public (publishable) keys - safe to store for frontend builds
VITE_SUPABASE_URL="https://wgvmfqkyksfyitmaehkl.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="<your-publishable-key-here>"

# Server-only secrets (do NOT commit)
# GROQ_API_KEY=""
# GROQ_API_SECRET=""
# TAVILY_API_KEY=""
# VITE_SUPABASE_SERVICE_ROLE_KEY=""  # server-only

# Example: create a .env.local (gitignored) with the above keys for local dev.
